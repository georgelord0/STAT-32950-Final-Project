# Verification summary

Command run successfully:

```bash
python3 final_version/final_analysis.py
```

Output comparison against existing `outputs/`:

- Generated files in `final_version/outputs/`: 48
- Existing files in root `outputs/`: 48
- Missing existing outputs in `final_version/outputs/`: 0
- Extra generated outputs: 0
- Generated CSV tables: 29
- CSV tables that exactly match existing outputs: 29
- CSV tables with content differences: 0
- Generated PNG figures: 19
- PNG figures with matching dimensions against existing outputs: 19/19

Notes:

- PNG files are regenerated by Matplotlib, so byte hashes may differ across environments even when dimensions and source data match.
- The unsuffixed PCA tables (`table_09_pca_variance.csv`, `table_10_pca_loadings.csv`) are compatibility full-sample PCA tables computed on the final cleaned analysis data and match the suffixed `_all` PCA tables.
